import { StyleSheet } from "react-native";

export default StyleSheet.create({
  component: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center"
  },
  buttonContainer: {
    margin: 10
  }
});
